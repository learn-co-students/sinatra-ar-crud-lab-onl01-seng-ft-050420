<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>All Articles</title>
  </head>
  <body>
    <h1>All Articles</h1>
    <% @articles.each do |a| puts a.title %>
      <p>Title: <%= a.title %></p>
      <p>Content: <%= a.content %></p>
    <% end %>
  </body>
</html>